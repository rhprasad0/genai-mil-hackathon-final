"""AO Radar fraud-mock Lambda package.

Deterministic, stateless, synthetic stand-in for the external compliance /
anomaly service the spec describes. It is not a real anomaly detector and
never reads or writes the database.
"""

__version__ = "0.1.0"
